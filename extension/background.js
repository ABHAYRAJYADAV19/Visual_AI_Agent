/**
 * Visual Activity Agent — Background Service Worker
 *
 * Responsibilities:
 * - Consent gate: all capture logic requires explicit user consent
 * - Badge state management (ON/OFF/PAUSED indicator)
 * - Event batching and flush scheduling
 * - Screenshot capture (visual consent required)
 * - API communication
 *
 * Full implementation in Phase 2+.
 */

// Placeholder — capture is OFF by default
console.log("[VAI] Background service worker loaded. Capture is OFF by default.");
